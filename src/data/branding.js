// Product names in one place, so a rename later is a one-file change.
// Keep these out of commit messages, the repo description and the README —
// the names are provisional and shouldn't be searchable on GitHub.
export const APP_NAME = 'OptiFocus'
export const GLASSES_NAME = 'FocusFrames'
